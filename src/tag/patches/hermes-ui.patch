diff -urN -x *.rej -x *.orig -x node_modules -x dist a/ui-tui/src/__tests__/brandingBanner.test.tsx b/ui-tui/src/__tests__/brandingBanner.test.tsx
--- a/ui-tui/src/__tests__/brandingBanner.test.tsx
+++ b/ui-tui/src/__tests__/brandingBanner.test.tsx
@@ -11,7 +11,10 @@
 import { DEFAULT_THEME, fromSkin } from '../theme.js'
 import type { SessionInfo } from '../types.js'
 
-const TAG_SKIN = resolve(import.meta.dirname, '../../../../tag/src/tag/assets/skins/tag-control.yaml')
+// Keep the fixture inside the published managed runtime. The old path escaped
+// the package into a developer checkout and made the shipped suite impossible
+// to run after extraction.
+const TAG_SKIN = resolve(import.meta.dirname, './fixtures/tag-control.yaml')
 
 const extractBlockScalar = (source: string, key: string) => {
   const marker = `${key}: |`
diff -urN -x *.rej -x *.orig -x node_modules -x dist a/ui-tui/src/__tests__/createSlashHandler.test.ts b/ui-tui/src/__tests__/createSlashHandler.test.ts
--- a/ui-tui/src/__tests__/createSlashHandler.test.ts
+++ b/ui-tui/src/__tests__/createSlashHandler.test.ts
@@ -74,7 +74,7 @@
 
     expect(createSlashHandler(ctx)('/update')).toBe(true)
     expect(ctx.gateway.gw.request).not.toHaveBeenCalled()
-    expect(ctx.transcript.sys).toHaveBeenCalledWith('exiting TUI to run update...')
+    expect(ctx.transcript.sys).toHaveBeenCalledWith('exiting TUI to run hermes update...')
 
     // Advance past the 100ms setTimeout
     vi.advanceTimersByTime(150)
@@ -524,6 +524,38 @@
     })
 
     expect(ctx.transcript.sys).not.toHaveBeenCalledWith('too late')
+  })
+
+  it('keeps a delayed session-scoped model result after a newer local slash', async () => {
+    let resolveModel: (value: { value: string }) => void
+    const rpc = vi.fn((method: string) => {
+      if (method === 'config.set') {
+        return new Promise<{ value: string }>(resolve => {
+          resolveModel = resolve
+        })
+      }
+
+      return Promise.resolve({})
+    })
+    const ctx = buildCtx({ gateway: { ...buildGateway(), rpc } })
+
+    const h = createSlashHandler(ctx)
+    expect(h('/model qa-new-model')).toBe(true)
+    expect(h('/help')).toBe(true)
+
+    resolveModel!({ value: 'qa-new-model' })
+    await vi.waitFor(() => {
+      expect(ctx.transcript.sys).toHaveBeenCalledWith('model → qa-new-model')
+    })
+    expect(getUiState().info?.model).toBe('qa-new-model')
+  })
+
+  it('prints usage for a bare personality command', () => {
+    const ctx = buildCtx()
+
+    expect(createSlashHandler(ctx)('/personality')).toBe(true)
+    expect(ctx.transcript.sys).toHaveBeenCalledWith('/personality <name>')
+    expect(ctx.gateway.rpc).not.toHaveBeenCalled()
   })
 
   it('dispatches command.dispatch with typed alias', async () => {
diff -urN -x *.rej -x *.orig -x node_modules -x dist a/ui-tui/src/__tests__/fixtures/tag-control.yaml b/ui-tui/src/__tests__/fixtures/tag-control.yaml
--- a/ui-tui/src/__tests__/fixtures/tag-control.yaml
+++ b/ui-tui/src/__tests__/fixtures/tag-control.yaml
@@ -0,0 +1,61 @@
+name: tag-control
+description: TAG control-room skin with indigo panels, cobalt prompts, and electric-blue accents.
+colors:
+  banner_border: "#44517A"
+  banner_title: "#8FA8FF"
+  banner_accent: "#52B6FF"
+  banner_dim: "#7587B3"
+  banner_text: "#E6EEFF"
+  ui_primary: "#8FA8FF"
+  ui_accent: "#52B6FF"
+  ui_border: "#44517A"
+  ui_text: "#E6EEFF"
+  ui_muted: "#7587B3"
+  ui_label: "#B7C7FF"
+  ui_ok: "#66D7A1"
+  ui_error: "#FF7D97"
+  ui_warn: "#7FD7FF"
+  prompt: "#8FA8FF"
+  input_rule: "#44517A"
+  response_border: "#52B6FF"
+  status_bar_bg: "#0D1324"
+  status_bar_text: "#DCE6FF"
+  status_bar_strong: "#52B6FF"
+  status_bar_dim: "#7587B3"
+  status_bar_good: "#66D7A1"
+  status_bar_warn: "#7FD7FF"
+  status_bar_bad: "#A88BFF"
+  status_bar_critical: "#FF7D97"
+  session_label: "#B7C7FF"
+  session_border: "#7587B3"
+  selection_bg: "#1D294A"
+  completion_menu_bg: "#0D1324"
+  completion_menu_current_bg: "#1D294A"
+  completion_menu_meta_bg: "#0A1020"
+  completion_menu_meta_current_bg: "#16254A"
+  shell_dollar: "#52B6FF"
+branding:
+  agent_name: "TAG Control"
+  agent_icon: "◈"
+  welcome: "TAG control plane online. Type your message or /help."
+  goodbye: "TAG control plane offline."
+  response_label: " ◈ TAG "
+  prompt_symbol: "›"
+  help_header: "[tag] Commands"
+tool_prefix: "│"
+banner_logo: |
+  [bold #52B6FF]████████╗ █████╗  ██████╗[/]
+  [bold #8FA8FF]╚══██╔══╝██╔══██╗██╔════╝[/]
+  [bold #B7C7FF]   ██║   ███████║██║  ███╗[/]
+  [bold #A88BFF]   ██║   ██╔══██║██║   ██║[/]
+  [bold #7FD7FF]   ██║   ██║  ██║╚██████╔╝[/]
+banner_hero: |
+  [#52B6FF]      ╭────────────╮[/]
+  [#8FA8FF]  ╭───┤ TAG        ├───╮[/]
+  [#7587B3]  │   ╰────┬───────╯   │[/]
+  [#B7C7FF]  │   ╭────┴────╮      │[/]
+  [#52B6FF]  ├───┤ CONTROL ├───┬──┤[/]
+  [#7587B3]  │   ╰────┬────╯   │  │[/]
+  [#A88BFF]  │   ╭────┴────╮   │  │[/]
+  [#66D7A1]  ╰───┤ AGENTS  ├───╯  │[/]
+  [#7587B3]      ╰─────────╯      │[/]
diff -urN -x *.rej -x *.orig -x node_modules -x dist a/ui-tui/src/__tests__/virtualHeights.test.ts b/ui-tui/src/__tests__/virtualHeights.test.ts
--- a/ui-tui/src/__tests__/virtualHeights.test.ts
+++ b/ui-tui/src/__tests__/virtualHeights.test.ts
@@ -20,7 +20,7 @@
   it('uses compound user prompt width when estimating user message wrapping', () => {
     const msg: Msg = { role: 'user', text: 'x'.repeat(21) }
 
-    expect(estimatedMsgHeight(msg, 26, { compact: false, details: false, userPrompt: '❯' })).toBe(3)
+    expect(estimatedMsgHeight(msg, 26, { compact: false, details: false, userPrompt: '❯' })).toBe(4)
     expect(estimatedMsgHeight(msg, 26, { compact: false, details: false, userPrompt: 'Ψ >' })).toBe(4)
   })
 
diff -urN -x *.rej -x *.orig -x node_modules -x dist a/ui-tui/src/app/createSlashHandler.ts b/ui-tui/src/app/createSlashHandler.ts
--- a/ui-tui/src/app/createSlashHandler.ts
+++ b/ui-tui/src/app/createSlashHandler.ts
@@ -19,7 +19,12 @@
     const parsed = parseSlashCommand(cmd)
     const argTail = parsed.arg ? ` ${parsed.arg}` : ''
 
+    // Preserve the historical flight guard for generic slash-worker output,
+    // but expose a session-only guard for independent local RPCs. A later
+    // read-only command must not discard a successful /model or /personality
+    // response that is still in flight.
     const stale = () => flight !== ctx.slashFlightRef.current || getUiState().sid !== sid
+    const staleSession = () => getUiState().sid !== sid
 
     const guarded =
       <T>(fn: (r: T) => void) =>
@@ -35,7 +40,7 @@
       }
     }
 
-    const runCtx: SlashRunCtx = { ...ctx, flight, guarded, guardedErr, sid, stale, ui }
+    const runCtx: SlashRunCtx = { ...ctx, flight, guarded, guardedErr, sid, stale, staleSession, ui }
 
     const found = findSlashCommand(parsed.name)
 
diff -urN -x *.rej -x *.orig -x node_modules -x dist a/ui-tui/src/app/slash/commands/session.ts b/ui-tui/src/app/slash/commands/session.ts
--- a/ui-tui/src/app/slash/commands/session.ts
+++ b/ui-tui/src/app/slash/commands/session.ts
@@ -75,7 +75,10 @@
       ctx.gateway
         .rpc<ConfigSetResponse>('config.set', { key: 'model', session_id: ctx.sid, value: modelValueForConfigSet(arg) })
         .then(
-          ctx.guarded<ConfigSetResponse>(r => {
+          (r: ConfigSetResponse | null) => {
+            if (ctx.staleSession() || !r) {
+              return
+            }
             if (!r.value) {
               return ctx.transcript.sys('error: invalid response: model switch')
             }
@@ -87,7 +90,7 @@
               ...state,
               info: state.info ? { ...state.info, model: r.value! } : { model: r.value!, skills: {}, tools: {} }
             }))
-          })
+          }
         )
     }
   },
@@ -142,7 +145,7 @@
     name: 'personality',
     run: (arg, ctx) => {
       if (!arg) {
-        return
+        return ctx.transcript.sys('/personality <name>')
       }
 
       ctx.gateway.rpc<ConfigSetResponse>('config.set', { key: 'personality', session_id: ctx.sid, value: arg }).then(
diff -urN -x *.rej -x *.orig -x node_modules -x dist a/ui-tui/src/app/slash/types.ts b/ui-tui/src/app/slash/types.ts
--- a/ui-tui/src/app/slash/types.ts
+++ b/ui-tui/src/app/slash/types.ts
@@ -9,6 +9,7 @@
   sid: null | string
   slashFlightRef: MutableRefObject<number>
   stale: () => boolean
+  staleSession: () => boolean
   ui: UiState
 }
 
diff -urN -x *.rej -x *.orig -x node_modules -x dist a/ui-tui/src/app/useInputHandlers.ts b/ui-tui/src/app/useInputHandlers.ts
--- a/ui-tui/src/app/useInputHandlers.ts
+++ b/ui-tui/src/app/useInputHandlers.ts
@@ -485,12 +485,17 @@
 
     if (key.ctrl && ch.toLowerCase() === 'c') {
       if (live.busy && live.sid) {
+        // Keep the busy state visible while queued work remains. Clearing it
+        // here lets the next queued prompt render a second user bubble before
+        // the controller has actually started that turn.
+        const keepBusy = cRefs.queueRef.current.length > 0
+
         return turnController.interruptTurn({
           appendMessage: actions.appendMessage,
           gw: gateway.gw,
           sid: live.sid,
           sys: actions.sys
-        })
+        }, { keepBusy })
       }
 
       if (cState.input || cState.inputBuf.length) {
@@ -500,11 +505,14 @@
       return actions.die()
     }
 
-    if (isAction(key, ch, 'd')) {
+    // Some macOS terminals send the documented Ctrl shortcuts as raw control
+    // bytes rather than Cmd/meta key events. Handle them before TextInput so
+    // the control letter can never be inserted into the draft.
+    if (isAction(key, ch, 'd') || (isMac && key.ctrl && ch.toLowerCase() === 'd')) {
       return actions.die()
     }
 
-    if (isAction(key, ch, 'l')) {
+    if (isAction(key, ch, 'l') || (isMac && key.ctrl && ch.toLowerCase() === 'l')) {
       clearSelection()
       forceRedraw(terminal.stdout ?? process.stdout)
 
diff -urN -x *.rej -x *.orig -x node_modules -x dist a/ui-tui/src/components/branding.tsx b/ui-tui/src/components/branding.tsx
--- a/ui-tui/src/components/branding.tsx
+++ b/ui-tui/src/components/branding.tsx
@@ -45,9 +45,10 @@
 // Terminals can't scale glyphs, so "responsive" means picking a layout that
 // fits the available columns. Thresholds are picked so each tier reads
 // comfortably without forcing wrap or truncation drift on box-drawing edges.
-const TAG_FULL = 'Nous Research · Messenger of the Digital Gods'
-const TAG_MID = 'Messenger of the Digital Gods'
-const TAG_TINY = 'Nous Research'
+const _isTag = process.env.HERMES_BIN_LABEL?.trim().toLowerCase() === 'tag'
+const TAG_FULL = _isTag ? 'TAG · Terminal Agent Gateway' : 'Nous Research · Messenger of the Digital Gods'
+const TAG_MID = _isTag ? 'Terminal Agent Gateway' : 'Messenger of the Digital Gods'
+const TAG_TINY = _isTag ? 'TAG' : 'Nous Research'
 const HIDE_BELOW = 34
 const COMPACT_FROM = 58
 
